const defaults = {
  hideSlider: false,
  direction: true, // 'horizontal' or 'vertical',
  height: 2,
  width: 2,
  backgroundColor: '#000',
  scrollBar: false,
  tabContentFn: false,
};

export default defaults;
